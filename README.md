# theoperativegroup.com — built site (preview)

This repository contains **only the generated output** of the rebuild, published so it can be
looked at. The source, the fact base and the review log live in a private repository.

- 165 pages + 36 redirect stubs
- Nothing here is live at theoperativegroup.com; DNS is untouched
- Preview is served from a subdirectory, so it is built with `BASE=/the-operative-group-site`

Built by The Operative Group. Do not edit these files by hand — they are regenerated.
